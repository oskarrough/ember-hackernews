
import { urlHost } from 'ember-hackernews/helpers/url-host';
import { module, test } from 'qunit';

module('Unit | Helper | url host');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = urlHost([42]);
  assert.ok(result);
});

